import React from 'react';
import PaymentRetry from '../components/PaymentRetry';

/**
 * RetryPayment page wrapper
 * This component wraps the PaymentRetry component for use as a route
 */
const RetryPayment = () => {
  return <PaymentRetry />;
};

export default RetryPayment;